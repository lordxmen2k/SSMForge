"""Pytest configuration for SSMForge tests.

Empty placeholder. basetemp is configured via pyproject.toml
[tool.pytest.ini_options]. This file exists so pytest finds the tests/
directory and to leave a hook for future fixture definitions.
"""
